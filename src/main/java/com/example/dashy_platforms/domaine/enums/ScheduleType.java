package com.example.dashy_platforms.domaine.enums;

public enum ScheduleType {
    DAILY,           // Chaque jour
    WEEKLY,          // Chaque semaine (jour spécifique)
    MONTHLY,         // Chaque mois (jour spécifique)
    INTERVAL,        // Intervalle personnalisé (heures/minutes)
    ONCE
}
